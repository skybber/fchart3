# FChart configuration file for

# Show star labels
show_star_labels=True

# Show Flamsteed star designation
show_flamsteed=True

# Show legend with magnitude scale
show_mag_scale_legend=True

# Show legend with map scale
show_map_scale_legend=True

# Show legend with orientation
show_orientation_legend=True

# Show legend with Deep Sky Objects (DSO)
show_dso_legend=False

# Show legend with coordinates
show_coords_legend=False

# Show field border
show_field_border=True

# Show equatorial grid
show_equatorial_grid=False

# Show constellation shapes
show_constellation_shapes=True

# Show constellation borders
show_constellation_borders=True

# Show Deep Sky Objects (DSO)
show_deepsky=True

# Show Simple Milky Way
show_milky_way=True

# Show enhanced (shaded) Milky Way with fading
show_enhanced_milky_way=False

# Show outlines of nebulae
show_nebula_outlines=True

# Show star colors according spectral type
star_colors=False

# Background color (RRGGBB format)
background_color=FFFFFF

# Drawing color (RRGGBB format)
draw_color=000000

# Label color (RRGGBB format)
label_color=202020

# Constellation lines color (RRGGBB format)
constellation_lines_color=80a0c0

# Constellation border color (RRGGBB format)
constellation_border_color=b0a020

# Highlighted constellation border color (RRGGBB format)
constellation_hl_border_color=FFF38C

# Nebula outline color (RRGGBB format)
nebula_color=005000

# Galaxy color (RRGGBB format)
galaxy_color=500000

# Star cluster color (RRGGBB format)
star_cluster_color=505000

# Galaxy cluster color (RRGGBB format)
galaxy_cluster_color=505050

# Milky Way color (RRGGBB format)
milky_way_color=d8d8d8

# Grid color (RRGGBB format)
grid_color=40505F

# Telrad color (RRGGBB format)
telrad_color=800000

# Constellation lines width
constellation_linewidth=0.5

# Constellation border width
constellation_border_linewidth=0.3

# Nebula outline width
nebula_linewidth=0.2

# Open cluster lines width
open_cluster_linewidth=0.3

# Galaxy cluster lines width
galaxy_cluster_linewidth=0.2

# DSO lines width
dso_linewidth=0.2

# Milky Way lines width
milky_way_linewidth=0.2

# Legend lines width
legend_linewidth=0.2

# Grid lines width
grid_linewidth=0.2

# Spece between star and constellation line
constellation_linespace=1.5

# No margin
no_margin=False

# Font
font=Arial

# Font size
font_size=3.0

# Legend font scale
legend_font_scale=1.75

# Enhanced Milky Way fade. The configuration contains 6 values that define a linear transformation of the enhanced Milky Way color
# from RGB space (0, 0, 0) to RGB space (1, 1, 1). The transformation is defined by the following formula:
# out = (in[0] + mw_color[0] * in[1], in[2] + mw_color[1] * in[3], in[4] + mw_color[2] * in[5])
# Each value in the tuple represents the contribution of the original RGB color channels to the final color, with mw_color as the color of enhanced Milky Way
enhanced_milky_way_fade=(0.0, 0.4, 0.0, 0.4, 0.0, 0.4)

# Telrad lines width
telrad_linewidth=0.3

# Bayer label font scale
bayer_label_font_scale=1.2

# Bayer label font style (normal/italic/bold)
bayer_label_font_style=normal

# Flamsteed label font scale
flamsteed_label_font_scale=0.9

# Flamsteed label font style (normal/italic/bold)
flamsteed_label_font_style=normal

# Display only Flamsteed star numbers (without constellation)
flamsteed_numbers_only=False

# FontStyle for DSO labels
dso_label_font_style=normal
